apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - frontend.yaml
  - otel-collector.yaml
  - trace-streamer.yaml
  - network-policy.yaml
  # db-external-secret.yaml requires External Secrets Operator (bootstrap step)
