# Azure Workload Identity annotation
# The client-id is patched by ArgoCD ApplicationSet with the actual managed identity
apiVersion: v1
kind: ServiceAccount
metadata:
  name: api
  annotations:
    azure.workload.identity/client-id: "AZURE_CLIENT_ID"
  labels:
    azure.workload.identity/use: "true"
